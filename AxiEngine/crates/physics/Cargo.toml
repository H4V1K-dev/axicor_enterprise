[package]
name = "physics"
version = "0.1.0"
edition = "2021"
description = "Layer 0 core domain mathematics and physics primitives for AxiEngine"

[dependencies]
types = { path = "../types" }

[dev-dependencies]
static_assertions = "=1.1.0"

[features]
default = []
std = []
