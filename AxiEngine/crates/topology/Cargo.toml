[package]
name = "topology"
version = "0.1.0"
edition = "2021"
license.workspace = true
publish = false

[dependencies]
types = { path = "../types" }
config = { path = "../config" }
layout = { path = "../layout" }
physics = { path = "../physics" }

[dev-dependencies]
static_assertions = "=1.1.0"
